/* Copy to station_config.h (git-ignored) and set your own station data. */
#pragma once
#define APRS_MYCALL      "N0CALL"   /* your callsign */
/* APRS-IS passcode for APRS_MYCALL. -1 = receive-only (no upload). */
#define APRSIS_PASSCODE  -1
